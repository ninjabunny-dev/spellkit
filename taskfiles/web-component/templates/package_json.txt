{
  "name": "{{project_name}}",
  "version": "0.0.1",
  "description": "Web Components UI Library",
  "main": "src/index.js",
  "module": "src/index.js",
  "type": "module",
  "scripts": {
    "test:unit": "mocha \"src/**/tests/*.test.js\" --recursive --require ./test-setup.js",
    "test:e2e": "playwright test",
    "test:integration": "echo 'Integration tests not yet implemented'",
    "test:watch": "mocha \"src/**/tests/*.test.js\" --recursive --require ./test-setup.js --watch",
    "test": "npm run test:unit && npm run test:integration && npm run test:e2e"
  },
  "repository": {
    "type": "git",
    "url": "{{ git_url }}"
  },
  "keywords": [
    "web-components"
  ],
  "author": "{{ auther }}",
  "license": "MIT",
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.0.11",
    "@playwright/test": "^1.57.0",
    "chai": "^6.2.1",
    "jsdom": "^27.2.0",
    "mocha": "^11.7.5"
  }
}
